### <a name="_b7urdng99y53"></a>**Название задачи:** Заявка на кредит онлайн

### <a name="_hjk0fkfyohdk"></a>**Автор:** Лукьянов Антон

### <a name="_uanumrh8zrui"></a>**Дата:** 07.12.2025



## Задание 7. Схемы коллекций MongoDB

- Коллекция orders

```javascript
{
   _id: ObjectId,                   // Уникальный идентификатор заказа
   customer_id: ObjectId,           // Идентификатор клиента
   order_date: Date,                // Дата и время оформления заказа
   items: [                         // Список заказанных товаров
   {
   product_id: ObjectId,
   name: String,
   price: Decimal128,               // Цена на момент заказа
   quantity: Number,   
   }
   ],
   status: String,                  // Статус заказа
   total_amount: Decimal128,        // Общая сумма заказа
   geo_zone: String,                // Геозона заказа
}

// Шард-ключ
sh.shardCollection("db.orders", {customer_id: 1})

// Дополнительные индексы
db.orders.createIndex({geo_zone: 1})
```

Шард-ключ: {customer_id}. Метод шардирования: "Хэшированное шардирование"

Альтернативный шард-ключ: {geo_zone, customer_id} - если нужна локализация по регионам

Так как основная операция поиск истории заказов, то запросы по customer_id будут локализованы на одном шарде.
Вариант шард-ключа с geo_zone позволит получать быстрее историю заказов для пользователей с шарда расположенного в их регионе, но для пользователей активно покупающих товары в разных регионах, история заказов будет собирать с разных шардов, что снизит скорость. 
Но вероятно таких пользователей немного.  


- Коллекция products

```javascript
{
  _id: ObjectId,                   // Уникальный идентификатор товара
  name: String,                    // Наименование
  category: String,                // Категория товара
  price: Decimal128,               // Текущая цена
  stock: {                         // Остатки по геозонам
    geo_zone_1: Number,
    geo_zone_2: Number,
    // ...
  },
  attributes: {                    // Дополнительные атрибуты
    color: String,
    size: String,
    // другие атрибуты
  }
}

// Шард-ключ
sh.shardCollection("db.products", {category: 1, price: 1})
```
Основные операции:
Частые обновления остатков при покупках.
Поиск товаров по категориям и фильтрация по диапазону цен.
Описание товара на странице продукта.

Шард-ключ: {category, price}. Метод шардирования: Хэшированное шардирование + Диапазонное шардирование

Так как основная операция поиск по категориям, то category помогает группировать товары одной категории на одном шарде.
Но требуется еще фильтрация по диапазону цен. Добавление _price в ключ позволит группировать товары одной категории, но разных ценовых диапазонах на разных шардах.  


- Коллекция carts

```javascript
{
  _id: ObjectId,                   // Уникальный идентификатор корзины
  user_id: ObjectId,               // Для авторизованных пользователей
  session_id: String,              // Для гостей
  items: [                         // Товары в корзине
    {
      product_id: ObjectId,
      quantity: Number,      
    }
  ],
  status: String,                  // Статус корзины "active", "ordered", "abandoned"
  created_at: Date,                // Дату и время создания 
  updated_at: Date,                // Дату и время последнего обновления
  expires_at: Date                 // Время удаления (TTL)
}


// Шард-ключ
// Вариант 1: Для пользователей
db.carts.createIndex({user_id: 1, status: active})
// Вариант 2: Для гостей
db.carts.createIndex({session_id: 1, status: active})

// TTL индекс для автоматической очистки
db.carts.createIndex({updated_at: 1}, {expireAfterSeconds: 2592000}) // 30 дней
```
Шард-ключ: {session_id, status} для гостей и {user_id, status} для авторизованных пользователей

Быстрый доступ к корзине по user_id или session_id.
Операция слияния корзин будет затрагивать максимум 2 шарда.


## Задание 8. Выявление и устранение «горячих» шардов

### Методы устранения:
- Добавить кэширование (Redis) для популярных категорий товаров.
- Добавить CDN для разных геозон
- Добавить подкатегории или префиксное шардирование для популярных категорий для перебалансировки
- Изменить диапазоны цен
- Ручная перебалансировка

```
const cacheHotProducts = async () => {
  const hotProducts = await db.products.aggregate([
    { $match: { category: "Электроника" } },
    { $sort: { viewCount: -1 } },
    { $limit: 1000 }
  ]).toArray();
  
  await redis.set("hot_products", JSON.stringify(hotProducts), "EX", 300);
};
```


### Стратегия выявления и устранения «горячих» шардов
На этапе проектирования анализировать распределение данных перед выбором шард-ключа
```javascript
// Анализ распределения данных по категориям
db.products.aggregate([
  { $group: { _id: "$category", count: { $sum: 1 } } },
  { $sort: { count: -1 } },
  { $project: { 
    category: "$_id", 
    count: 1,
    percentage: { $multiply: [{ $divide: ["$count", db.products.count()] }, 100] }
  }}
])

// Анализ нагрузки по категориям (сбор статистики запросов)
db.system.profile.find({ 
  ns: "db.products", 
  op: { $in: ["query", "update", "getmore"] }
}).sort({ ts: -1 }).limit(1000)
```

Добавить метрики мониторинга горячих шардов

1. Метрики нагрузки на шарды:
- Операции чтения/записи в секунду (IOPS) на каждом шарде
- Загрузка CPU на каждом шарде
- Использование памяти на каждом шарде

2. Метрики распределения:
- Количество чанков на шард
- Размер данных на шард
- Количество документов на шард
- Размер индексов на шард

3. Метрики производительности:
- Операций чтения/записи в секунду по шардам
- Загрузка CPU по шардам
- Использование памяти по шардам
- Задержка запросов (p95, p99)

4. Метрики балансировки:
- Количество миграций чанков
- Время балансировки
- Ошибки балансировки

5. Метрики запросов:
- Топ-10 самых частых запросов
- Самые медленные запросы
- Распределение запросов по типам операций


Составить dashbord (Prometheus + Grafana с mongodb_exporter).
Настроить алерты при дисбалансе > 30%.


Так как большая часть продуктов популярной и большой категории попала на один шард, это вызвало перегрузку.
Остатки продуктов распределены по геозонам.
Можно применить zoned aware sharding.
Для каждой геозоны создать зону.
В products в корень нужно добавить поле которое будет определять зону, например 
primary_geo_zone: String

Тогда можно применить шард-ключ на {primary_geo_zone, category}
Данные будут разделены по геозонам, что позволит распределить нагрузку от популярной категории "Электроника" по нескольким шардам.

Запросы, которые включают фильтр по геозоне, будут выполняться только на соответствующем шарде (если в запросе указана геозона).
Но если в запросе не указана геозона, то запрос будет scatter-gather по всем шардам, что плохо.
```javascript
sh.addShardToZone("shard01", "Moscow");
sh.addShardToZone("shard02", "Omsk");
sh.addShardToZone("shard03", "Vladivostok");

sh.updateZoneKeyRange(
"db.products",
{ primary_geo_zone: "Moscow", category: "electronica" },
{ primary_geo_zone: "Vladivostok", category: "electronica" },
"US"
);
```



## Задание 9. Настройка чтения с реплик и консистентность

Коллекция: products

| **Операция чтения**                                       | **Primary/Secondary**  | **Допустимая задержка**  | **Обоснование**                                                                                 |
|:----------------------------------------------------------|:----------------------:|:------------------------:|:------------------------------------------------------------------------------------------------|
| Поиск товаров по категориям и фильтрация по диапазону цен |       Secondary        |           10c            | Данные в каталоге обновляются не так часто, и небольшая задержка допустима.                     |
| Описание товара на странице продукта                      |       Secondary        |           10c            | Описание товара меняется редко.                                                                 |
| Проверка остатка товара при добавлении в корзину          |        Primary         |            0c            | Нельзя допустить продажу недоступного товара, поэтому необходимо актуальное значение остатка.   |


Коллекция: orders

| **Операция чтения**                                | **Primary/Secondary** | **Допустимая задержка** | **Обоснование**                                                                                                                                         |
|:---------------------------------------------------|:---------------------:|:-----------------------:|:--------------------------------------------------------------------------------------------------------------------------------------------------------|
| Поиск истории заказов конкретного пользователя     |       Secondary       |           10c           | История заказов не меняется часто, и небольшая задержка допустима.                                                                                      |
| Отображение статуса заказа (после создания заказа) |        Primary        |           0c            | Пользователь должен видеть актуальный статус заказа, особенно если он только что его создал.                                                            |
| Отображение статуса заказа (общее)                 |       Secondary       |           10c           | Если пользователь просматривает заказ, который был сделан некоторое время назад, то можно использовать secondary так как данные уже перетекут на него.  |


Коллекция: carts

| **Операция чтения**                                                                                    | **Primary/Secondary** | **Допустимая задержка** | **Обоснование**                                                                                                 |
|:-------------------------------------------------------------------------------------------------------|:---------------------:|:-----------------------:|:----------------------------------------------------------------------------------------------------------------|
| Получение текущей корзины по фильтру { session_id, status:"active" } или { user_id, status:"active" }  |        Primary        |           0c            | Корзина может часто меняться, и необходимо получить актуальное состояние для отображения и дальнейших операций. |
| Получение корзины для слияния (гостевая корзина)                                                       |        Primary        |           0c            | При логине необходимо получить актуальную гостевую корзину, чтобы правильно объединить с пользовательской.      |
| Чтение корзины для отображения (например, при открытии страницы корзины)                               |        Primary        |           0c            | Пользователь должен видеть актуальную корзину, так как она может меняться с других устройств или вкладок.       |


## Миграция на Cassandra: модель данных, стратегии репликации и шардирования

### Обоснование 
Для интернет магазина критически важными с точки зрения целостности и скорости обработки являются:
1. Товары (products) - часто читаются, редко обновляются (кроме остатков). Требуется высокая доступность.

2. Заказы (orders) - часто пишутся, часто читаются. Требуется высокая доступность и целостность.

3. Корзины (carts) - часто обновляются, требуют низкой задержки. Целостность важна, но не так критична, как для заказов.

4. Пользовательские сессии (sessions) - временные данные, требуют высокой скорости записи/чтения, целостность не критична.

Так как при изменении остатков необходима ACID транзакция, то products не подходит для Cassandra.

| **Сущность**             | **Критичность целостности** | **Критичность скорости** | **Подходит для Cassandra** | **Обоснование**                                                                           |
|:-------------------------|:---------------------------:|:------------------------:|:--------------------------:|-------------------------------------------------------------------------------------------|
| Корзины (carts)          |           Средняя           |         Высокая          |             Да             | Временные данные, eventual consistency приемлема, чтение по session_id/user_id                                        |
| Заказы (orders)          |        Очень высокая        |         Высокая          |            Нет             | Высокая скорость записи, ACID транзакции                                                  |
| История заказов          |           Высокая           |         Средняя          |             Да             | Чтение по пользователю, append-only данные                                                |
| Товары (products)        |           Высокая           |         Высокая          |            Нет             | Каталог товаров хранить остатки, а их консистентность критична. Требуются ACID транзакции |
| Пользовательские сессии  |           Низкая            |      Очень высокая       |             Да             | Ключ-значение, TTL, высокая доступность критична                                          |

### Концептуальная модель данных в Cassandra 

``` cql
CREATE TABLE carts (
    cart_id UUID,
    user_id UUID,
    session_id TEXT,
    status TEXT,  -- 'active', 'ordered', 'abandoned'
    items MAP<UUID, INT>,  -- product_id -> quantity
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    expires_at TIMESTAMP,
    PRIMARY KEY ((cart_id, user_id, session_id))
) WITH default_time_to_live = 2592000;  -- 30 дней TTL
```

Для зарегистрированных пользователей: partition key = user_id, для гостей: partition key = session_id.

``` cql
CREATE TABLE user_sessions (
    session_id TEXT,
    user_id UUID,
    session_data TEXT,  -- JSON или сериализованные данные
    created_at TIMESTAMP,
    last_activity TIMESTAMP,
    user_agent TEXT,
    ip_address INET,
    PRIMARY KEY (session_id)
) WITH default_time_to_live = 86400;  -- 24 часа TTL

```

Partition key: session_id

### Cтратегии беспечения целостности данных


| **Сущность**             |    **Стратегия**    | **Обоснование**                |
|:-------------------------|:-------------------:|:-------------------------------|
| Корзины (carts)          |     Read Repair     | Важна быстрая консистентность  |
| История заказов          | Anti-Entropy Repair | Данные читаются редко          |
| Пользовательские сессии  |   Hinted Handoff    | Данные временные               |            
